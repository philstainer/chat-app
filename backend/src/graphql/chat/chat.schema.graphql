type Chat {
  _id: ID!
  participants: [User!]!
  lastMessage: Message
}

type Query {
  chats: [Chat!]!
}

type Mutation {
  createChat(input: CreateChatInput!): Chat!
}

type Subscription {
  chatCreated: Chat!
}

input CreateChatInput {
  participants: [ID!]!
}
