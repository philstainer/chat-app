type Chat {
  _id: ID!
  participants: [User!]!
  lastMessage: Message
}

type Query {
  chats: [Chat!]!
}

type Mutation {
  createChat(input: CreateChatInput!): Chat!
}

type Subscription {
  _empty: String
}

input CreateChatInput {
  participants: [ID!]!
}
