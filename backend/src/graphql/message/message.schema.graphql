type Message {
  _id: ID!
  text: String
  sender: User!
}

type Query {
  messages(input: MessagesInput): [Message!]!
}

type Mutation {
  _empty: String
}

type Subscription {
  _empty: String
}

input MessagesInput {
  chatId: ID!
  skip: Int
  limit: Int
}
